import React from 'react'
import ReactDOM from 'react-dom/client'
import TempoRunApp from './TempoRun.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TempoRunApp />
  </React.StrictMode>,
)
